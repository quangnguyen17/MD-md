# update npm
sudo npm install --global npm@latest
# update my npm packages
sudo npm install --global expo-cli sharp-cli firebase-tools nodemon typescript mocha pnpm
# update brew packages
brew update
